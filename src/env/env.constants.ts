export const SWAGGER_DOCUMENTATION_PATH = 'api/documentation';

export const SWAGGER_JSON_PATH = '/api-json';

export enum AppPort {
  LOCAL = 4002,
}

export enum AppEnvironment {
  LOCAL = 'LOCAL',
  PRODUCTION = 'PRODUCTION',
}

export enum EnvVariable {
  NODE_ENV = 'NODE_ENV',
}
